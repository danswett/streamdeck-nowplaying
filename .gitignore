node_modules/
dist/
*.streamDeckPlugin

# Build output, reproduced by `npm run all`.
com.bad-duck.nowplaying.sdPlugin/bin/
com.bad-duck.nowplaying.sdPlugin/logs/
com.bad-duck.nowplaying.sdPlugin/imgs/actions/*.png
com.bad-duck.nowplaying.sdPlugin/imgs/plugin/*.png

# .NET
bridge/bin/
bridge/obj/

# Local feed override. Kept out of the repo because it is machine-specific and
# may name an internal package proxy; see bridge/NuGet.config.example.
bridge/NuGet.config
